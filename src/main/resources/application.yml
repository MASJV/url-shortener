spring:
  application:
    name: url-shortener
  datasource:
    url: jdbc:mysql://database-1.co5i4acosz00.us-east-1.rds.amazonaws.com:3306/url_shortener
    username: admin
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # Options: none, update, create, create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8080

logging:
  level:
    root: INFO
    org.example: DEBUG
    org.hibernate: DEBUG
    com.example: DEBUG
